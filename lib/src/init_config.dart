class InitConfig {
  String? error;
  String? countryCode;
  bool isConsentRequired;
  bool isTestMode;

  InitConfig(
    this.error,
    this.countryCode,
    this.isConsentRequired,
    this.isTestMode,
  );
}
